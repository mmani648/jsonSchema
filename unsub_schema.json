{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "unsub": {
      "type": "boolean",
      "description": "True if the email is requesting unsubscribe, opt out, or removal"
    },
    "cleanemail": {
      "type": "string",
      "description": "A cleaned version of the unsubscribe request text. Empty if unsub is false."
    }
  },
  "required": ["unsub", "cleanemail"],
  "additionalProperties": false
}
